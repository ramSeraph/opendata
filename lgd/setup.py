import setuptools


setuptools.setup(
    name='lgd',
    version='0.0.1',
    description='LGD scraping workflow package.',
    install_requires=[],
    package_dir={'': '.'},
    packages=setuptools.find_packages(where='.'),
    )
